name: "vlc"
description: "Completions for vlc"
# YAML is very slow (it takes up to seven seconds to parse the YAML file)
# so we use JSON here
bash_args: ["vlc.json"]
fish_args: ["vlc.json", "--repeatable-options=True"]
zsh_args:  ["vlc.json"]
